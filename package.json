{
  "name": "draft",
  "private": true,
  "version": "0.0.0",
  "description": "Draft — browser drafting tools (GitHub Pages runtime).",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  }
}
